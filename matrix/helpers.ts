// import solparse from 'solparse';

// const parsed = solparse.parse(hackMeSource);
// const importStatements = parsed.body.filter(node => node.type === 'ImportDeclaration');

// importStatements.forEach(importStatement => {
//     const importedContractName = importStatement.alias || importStatement.from.split('/').pop().split('.')[0];
//     const importedContractPath = importStatement.from;
//     console.log(`Contract Name: ${importedContractName}`);
//     console.log(`Contract Path: ${importedContractPath}`);
// });
